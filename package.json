{
  "name": "editorial",
  "private": true,
  "scripts": {
    "compile": "pnpm --recursive --parallel compile",
    "compile:watch": "pnpm run --recursive --parallel compile:watch",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.4"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "volta": {
    "node": "22.17.0",
    "pnpm": "10.28.0"
  }
}
