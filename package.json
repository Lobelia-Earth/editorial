{
  "name": "editorial",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "compile": "pnpm --recursive --parallel compile",
    "compile:watch": "pnpm run --recursive --parallel compile:watch"
  },
  "engines": {
    "node": ">=22.16.0",
    "pnpm": "10.11.1"
  },
  "volta": {
    "node": "22.16.0",
    "pnpm": "10.11.1"
  }
}
