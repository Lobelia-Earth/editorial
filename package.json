{
  "name": "editorial",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "compile": "pnpm --recursive --parallel compile",
    "compile:watch": "pnpm run --recursive --parallel compile:watch"
  },
  "engines": {
    "node": ">=22.13.1",
    "pnpm": "10.2.0"
  },
  "volta": {
    "node": "22.13.1",
    "pnpm": "10.2.0"
  }
}
